#include<stdio.h>
int main(){
    float a ;
    printf("Enter the decimal number:");
    scanf("%f",&a);
    int b = a ;
    float r = a - b ;
    printf("The fractional part of the number is %f",r);
    return 0;
}
