#include<stdio.h>
int main()
{
    int n,ld,sum=0;
    printf("Enter a number :");
    scanf("%d",&n);
    while(n>0)
    {
        ld=n%10;
        sum=ld+sum;
        n=n/10;
    }
    printf("Sum of the digits is %d",sum);
    return 0;
}
